<Window x:Class="PL_GUI.ReadLogWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:PL_GUI"
        mc:Ignorable="d"
        Title="ReadLogWindow" Height="400" Width="400">
    <Grid>
        <ListBox Margin="20,62,20,20" Name="LogBox">
            <ListBox.ItemTemplate>
                <DataTemplate>
                    <StackPanel Orientation="Horizontal">
                        <TextBlock Text="{Binding}" />
                    </StackPanel>
                </DataTemplate>
            </ListBox.ItemTemplate>
        </ListBox>
        <Button x:Name="MainMenu" FontSize="10" Click="MainMenu_Click"  Margin="306,0,15.333,329.667" Height="31" VerticalAlignment="Bottom">Main Menu</Button>
        <Button x:Name="Back" FontSize="10" Click="Back_Click"  Margin="10,10,311.333,329.667">Back</Button>
    </Grid>
</Window>
